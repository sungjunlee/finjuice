"""Canonical local JSON statement adapter (issue #448).

Import ``finjuice.pipeline.statements.canonical`` directly; this package
intentionally re-exports nothing so that importing it cannot pull the storage
and assets packages into a circular initialization order.
"""
